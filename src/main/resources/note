- elasticsearch暂未实现，以为es无法启动成功
- 后面的所有都没有完成，后面完成，先进行面试



HyperLogLog:
    采用一种基数算法，用于完成独立总数的统计
    占据空间小，无论统计多少个数据，只占12k的内存空间
    不精确的统计算法，标准误差为0.81%

Bitmap:
    不是一种独立的数据结构，实际上就是字符串
    支持按位存取数据信息，可以将其看成是byte数组
    适合存储索引大量连续数据的布尔值

使用的场景：
    UV（unique visitor）
        - 独立访客，需要通过用户ip排重统计数据(只要看过就行，不管有没有登录)
        - 每次访问都要进行统计
        - Hyperloglog，性能好，并且存储空间小

    DAU（daily active user）
        - 日活跃用户，需要通过用户id排重统计数据(只有用户登录了才可以进行统计)
        - 访问过一次，则认为其活跃
        - Bitmap，性能好，并且可以统计精确的结果(因为userId是整数，所以只需要使用bitmap中的位来存储登录状态即可)
